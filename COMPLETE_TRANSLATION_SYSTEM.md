# 完整多语言翻译系统文档

## ✅ 已完成的工作

### 1. 统一翻译系统
- ✅ 创建了扁平化的 key 结构 (`section.item` 格式)
- ✅ 支持三种语言：`en`, `zh-CN`, `zh-TW`
- ✅ 所有翻译键都在 `src/lib/translations.ts` 中定义

### 2. TranslationContext 和 Hook
- ✅ 创建了 `TranslationContext` 和 `useTranslation()` hook
- ✅ 支持从 API 动态加载翻译
- ✅ 自动持久化语言选择到 localStorage
- ✅ 跨标签页同步语言设置

### 3. 翻译管理界面
- ✅ 创建了 `/admin/translations` 页面
- ✅ 表格形式显示所有翻译
- ✅ 可编辑所有语言版本
- ✅ 搜索功能
- ✅ 添加/删除翻译键
- ✅ 保存到 JSON 文件

### 4. API 路由
- ✅ `/api/translations` - 公共读取接口
- ✅ `/api/admin/translations` - 管理员写入接口（需要认证）

### 5. 语言切换器修复
- ✅ 下拉菜单正确显示在按钮下方
- ✅ 自动调整位置防止溢出视口
- ✅ 移动端友好
- ✅ 点击外部自动关闭

### 6. 组件更新
- ✅ `_app.tsx` - 使用新的 TranslationProvider
- ✅ `Layout.tsx` - 使用新的 useTranslation
- ✅ `LanguageSwitch.tsx` - 完全重写，修复定位
- ✅ `index.tsx` - 首页已更新
- ✅ `auth/login.tsx` - 登录页已更新
- ✅ `profile/index.tsx` - 个人资料页已更新

## 📋 如何使用

### 在组件中使用翻译

```tsx
import { useTranslation } from '@/contexts/TranslationContext';

export default function MyComponent() {
  const { t } = useTranslation();
  
  return (
    <div>
      <h1>{t('common.appName')}</h1>
      <button>{t('common.save')}</button>
    </div>
  );
}
```

### 访问翻译管理界面

1. 以管理员身份登录
2. 访问 `http://localhost:3000/admin/translations`
3. 在表格中编辑翻译
4. 点击"保存翻译"按钮

## 🔑 翻译键命名规范

使用点分隔的层级结构：

```
common.*          - 通用文本
navbar.*          - 导航栏
auth.*            - 认证相关
home.*            - 首页
dashboard.*       - 控制台
profile.*         - 个人资料
application.*     - 申请表单
ai.*              - AI 相关
settings.*        - 设置
admin.*           - 管理后台
```

## ➕ 如何安全地添加新翻译键

### 方法一：通过管理界面（推荐）

1. 访问 `/admin/translations`
2. 点击"添加新翻译"
3. 输入键名（如：`new.feature.title`）
4. 填写三种语言的翻译
5. 点击"Add"
6. 点击"保存翻译"

### 方法二：直接编辑代码

1. 打开 `src/lib/translations.ts`
2. 在 `translations` 对象中添加：

```typescript
'new.feature.title': {
  en: 'New Feature',
  'zh-CN': '新功能',
  'zh-TW': '新功能',
},
```

3. 在组件中使用：`t('new.feature.title')`

## ⚠️ 重要规则

1. **所有硬编码文本必须替换**
   - ❌ 错误：`<span>保存</span>`
   - ✅ 正确：`<span>{t('common.save')}</span>`

2. **键名必须唯一且描述性**
   - ✅ 好：`auth.login.button`
   - ❌ 差：`btn1`

3. **所有语言必须提供翻译**
   - 至少提供英文
   - 缺失的会回退到英文

4. **使用扁平结构**
   - ✅ 正确：`t('home.features.autoFill.title')`
   - ❌ 错误：`t.home.features.autoFill.title`

## 🔄 迁移剩余组件

以下文件仍需要更新（使用旧系统）：

- `src/pages/dashboard.tsx`
- `src/pages/application/[applicationId].tsx`
- `src/components/AIGuidancePanel.tsx`
- `src/pages/settings.tsx`
- `src/pages/admin/templates.tsx`
- `src/pages/auth/register.tsx`
- 其他页面...

**更新模式：**
1. 将 `import { useLanguage } from '@/contexts/LanguageContext'` 
   改为 `import { useTranslation } from '@/contexts/TranslationContext'`
2. 将 `const { t } = useLanguage()` 
   改为 `const { t } = useTranslation()`
3. 将 `t.section.key` 
   改为 `t('section.key')`

## 📁 文件结构

```
src/
├── lib/
│   └── translations.ts              # 翻译数据定义（默认值）
├── contexts/
│   └── TranslationContext.tsx        # 翻译上下文
├── components/
│   └── LanguageSwitch.tsx           # 语言切换器（已修复）
└── pages/
    ├── api/
    │   ├── translations.ts          # 公共读取 API
    │   └── admin/
    │       └── translations.ts      # 管理员写入 API
    └── admin/
        └── translations.tsx          # 翻译管理界面
```

## 🚀 部署说明

### 首次部署

1. 系统会自动创建 `src/lib/translations.json`（首次保存时）
2. 如果文件不存在，API 会使用 `translations.ts` 中的默认值
3. 管理员可以通过界面添加/编辑翻译

### 更新翻译

1. 在 `/admin/translations` 界面编辑
2. 点击"保存翻译"
3. 翻译会保存到 `src/lib/translations.json`
4. 所有用户刷新页面后看到新翻译

## 🐛 故障排除

### 翻译不显示
- 检查键名是否正确
- 查看浏览器控制台警告
- 确认翻译数据中是否存在该键

### 语言切换不工作
- 检查 `TranslationProvider` 是否正确包裹应用
- 检查浏览器 localStorage
- 查看控制台错误

### 管理界面无法访问
- 确认已以管理员身份登录
- 检查 JWT token 是否有效
- 查看 API 响应错误

## 📊 翻译键完整列表

所有可用的翻译键都在 `src/lib/translations.ts` 中定义。主要分类：

- `common.*` - 20+ 个通用键
- `navbar.*` - 导航栏相关
- `auth.*` - 认证相关（登录、注册、错误）
- `home.*` - 首页（标题、特性、步骤、CTA）
- `dashboard.*` - 控制台相关
- `profile.*` - 个人资料相关
- `application.*` - 申请表单相关
- `ai.*` - AI 指导相关
- `settings.*` - 设置相关
- `admin.*` - 管理后台相关

## ✨ 下一步

1. 更新剩余组件使用新系统
2. 扫描所有文件查找硬编码文本
3. 添加缺失的翻译键
4. 测试所有页面的语言切换
5. 部署到生产环境

