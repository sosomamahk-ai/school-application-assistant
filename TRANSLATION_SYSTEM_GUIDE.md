# 多语言翻译系统使用指南

## 📋 系统概述

新的统一翻译系统使用扁平化的 key 结构，格式为：`"section.item"`

例如：
- `"login.button"` - 登录按钮
- `"navbar.home"` - 导航栏首页
- `"form.submit"` - 表单提交

## 🚀 快速开始

### 1. 更新 _app.tsx

系统已经自动更新，使用 `TranslationProvider` 替代了旧的 `LanguageProvider`。

### 2. 在组件中使用翻译

```tsx
import { useTranslation } from '@/contexts/TranslationContext';

export default function MyComponent() {
  const { t } = useTranslation();
  
  return (
    <button>{t('login.button')}</button>
  );
}
```

### 3. 访问翻译管理界面

1. 以管理员身份登录
2. 访问 `/admin/translations`
3. 在表格中编辑所有翻译
4. 点击"保存翻译"按钮

## 📝 添加新的翻译键

### 方法一：通过管理界面（推荐）

1. 访问 `/admin/translations`
2. 点击"添加新翻译"
3. 输入键名（如：`new.section.key`）
4. 填写三种语言的翻译
5. 点击"Add"按钮
6. 点击"保存翻译"

### 方法二：直接编辑代码

1. 打开 `src/lib/translations.ts`
2. 在 `translations` 对象中添加新键：

```typescript
export const translations: TranslationData = {
  // ... existing translations
  'new.section.key': {
    en: 'English text',
    'zh-CN': '简体中文',
    'zh-TW': '繁體中文',
  },
};
```

3. 在组件中使用：`t('new.section.key')`

## 🔧 迁移现有代码

### 旧代码（使用 useLanguage）
```tsx
import { useLanguage } from '@/contexts/LanguageContext';
const { t } = useLanguage();
<span>{t.common.save}</span>
```

### 新代码（使用 useTranslation）
```tsx
import { useTranslation } from '@/contexts/TranslationContext';
const { t } = useTranslation();
<span>{t('common.save')}</span>
```

## 📂 文件结构

```
src/
├── lib/
│   └── translations.ts          # 翻译数据定义
├── contexts/
│   └── TranslationContext.tsx    # 翻译上下文和 hook
├── components/
│   └── LanguageSwitch.tsx       # 语言切换组件（已修复下拉定位）
└── pages/
    ├── api/
    │   ├── translations.ts      # 公共翻译 API（读取）
    │   └── admin/
    │       └── translations.ts  # 管理翻译 API（写入）
    └── admin/
        └── translations.tsx     # 翻译管理界面
```

## 🎯 翻译键命名规范

使用点分隔的层级结构：

- `common.*` - 通用文本（保存、取消、加载等）
- `navbar.*` - 导航栏
- `auth.*` - 认证相关
- `home.*` - 首页
- `dashboard.*` - 控制台
- `profile.*` - 个人资料
- `application.*` - 申请表单
- `ai.*` - AI 相关
- `settings.*` - 设置
- `admin.*` - 管理后台

## ⚠️ 重要提示

1. **所有硬编码文本必须替换为 `t(key)`**
   - ❌ 错误：`<span>保存</span>`
   - ✅ 正确：`<span>{t('common.save')}</span>`

2. **键名必须唯一**
   - 使用描述性的键名
   - 遵循命名规范

3. **所有语言必须提供翻译**
   - 至少提供英文翻译
   - 缺失的翻译会回退到英文

4. **保存翻译后需要刷新页面**
   - 翻译保存在 `src/lib/translations.json`
   - 页面会自动重新加载翻译

## 🔍 查找缺失的翻译

如果看到翻译键名直接显示（如：`common.save`），说明：
1. 该键在翻译数据中不存在
2. 需要添加该翻译键

## 📊 翻译管理界面功能

- ✅ 表格形式显示所有翻译
- ✅ 可编辑所有语言版本
- ✅ 搜索功能
- ✅ 添加新翻译
- ✅ 删除翻译
- ✅ 保存到文件

## 🐛 故障排除

### 问题：翻译不更新
- 检查浏览器控制台是否有错误
- 确认已点击"保存翻译"按钮
- 刷新页面

### 问题：翻译键显示为键名
- 检查键名是否正确
- 确认翻译数据中是否存在该键
- 检查控制台警告信息

### 问题：语言切换不工作
- 检查 `TranslationProvider` 是否正确包裹应用
- 检查浏览器 localStorage 是否可用
- 查看浏览器控制台错误

## 📞 需要帮助？

如果遇到问题，请检查：
1. 浏览器控制台的错误信息
2. 网络请求是否成功（F12 → Network）
3. 翻译文件是否正确保存

