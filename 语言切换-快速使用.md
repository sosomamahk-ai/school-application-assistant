# ⚡ 语言切换 - 快速使用指南

## 🎯 用户使用（30秒学会）

### 在哪里切换语言？

你会在以下位置看到语言切换按钮（显示为 `🌐 中文` 或 `🌐 EN`）：

1. **首页**：右上角导航栏
2. **登录页**：右上角
3. **仪表板**：顶部导航栏
4. **其他页面**：顶部导航栏

### 如何切换？

```
点击 [🌐 中文] → 立即变成 [🌐 EN]
点击 [🌐 EN] → 立即变成 [🌐 中文]
```

**就这么简单！** ✨

### 特点

- ✅ **即时生效**：无需刷新页面
- ✅ **自动保存**：下次打开自动使用你的选择
- ✅ **全局同步**：切换后所有页面都会更新

---

## 👨‍💻 开发者使用（3分钟学会）

### 1. 在组件中使用翻译

```typescript
import { useLanguage } from '@/contexts/LanguageContext';

export default function MyComponent() {
  const { t } = useLanguage();
  
  return <h1>{t.common.appName}</h1>;
}
```

### 2. 添加语言切换按钮

```typescript
import LanguageSwitch from '@/components/LanguageSwitch';

// 简洁版（用于导航栏）
<LanguageSwitch variant="minimal" />

// 完整版（用于设置页）
<LanguageSwitch />
```

### 3. 添加新翻译

编辑 `src/config/translations.ts`：

```typescript
export const translations: Record<Language, Translations> = {
  zh: {
    mySection: {
      title: '我的标题',
      button: '点击这里'
    }
  },
  en: {
    mySection: {
      title: 'My Title',
      button: 'Click Here'
    }
  }
};
```

使用：

```typescript
<h1>{t.mySection.title}</h1>
<button>{t.mySection.button}</button>
```

---

## 📋 已翻译内容速查

| 分类 | 包含内容 | 使用方式 |
|------|---------|---------|
| **通用** | 登录、注册、保存、取消等 | `t.common.xxx` |
| **首页** | 标题、特性、步骤说明 | `t.home.xxx` |
| **登录** | 邮箱、密码、按钮 | `t.auth.login.xxx` |
| **注册** | 表单字段 | `t.auth.register.xxx` |
| **仪表板** | 申请列表、状态 | `t.dashboard.xxx` |
| **个人资料** | 字段标签 | `t.profile.xxx` |
| **AI** | AI提示、建议 | `t.ai.xxx` |

### 常用翻译示例

```typescript
// 按钮
{t.common.save}        // "保存" / "Save"
{t.common.cancel}      // "取消" / "Cancel"
{t.common.submit}      // "提交" / "Submit"

// 标题
{t.dashboard.title}    // "我的申请" / "My Applications"
{t.profile.title}      // "个人资料" / "Profile"

// 状态
{t.dashboard.draft}    // "草稿" / "Draft"
{t.dashboard.submitted} // "已提交" / "Submitted"
```

---

## 🚀 测试步骤

### 本地测试

1. **启动开发服务器**
   ```bash
   npm run dev
   ```

2. **打开浏览器**
   访问 `http://localhost:3000`

3. **测试切换**
   - 点击右上角语言按钮
   - 观察页面文字变化
   - 刷新页面，确认语言设置被保存

4. **测试不同页面**
   - 首页 → 切换 ✓
   - 登录页 → 切换 ✓
   - 仪表板 → 切换 ✓

---

## ⚠️ 注意事项

### ❌ 不要这样做

```typescript
// 硬编码文字
<h1>我的申请</h1>
<button>保存</button>
```

### ✅ 应该这样做

```typescript
// 使用翻译系统
<h1>{t.dashboard.title}</h1>
<button>{t.common.save}</button>
```

### 💡 例外情况

这些内容**不需要**翻译：
- 用户输入的数据（姓名、邮箱等）
- 数据库中的内容（学校名称、申请内容等）
- 日期和数字（会自动本地化）

```typescript
// 这些不翻译
<div>{user.name}</div>           // 用户姓名
<div>{school.name}</div>         // 学校名称
<div>{application.content}</div> // 申请内容
```

---

## 🔧 快速问题解决

### 问题：切换后文字没变？

**检查清单**：
- [ ] 是否使用了 `{t.xxx.xxx}` 格式？
- [ ] 翻译是否存在于 `translations.ts` 中？
- [ ] 组件是否导入了 `useLanguage`？

### 问题：报错 "useLanguage must be used..."

**解决**：确保 `_app.tsx` 中有 `<LanguageProvider>`

```typescript
// src/pages/_app.tsx
import { LanguageProvider } from '@/contexts/LanguageContext';

export default function App({ Component, pageProps }: AppProps) {
  return (
    <LanguageProvider>
      <Component {...pageProps} />
    </LanguageProvider>
  );
}
```

---

## 📦 文件位置

```
src/
├── config/
│   └── translations.ts          ← 所有翻译在这里
├── contexts/
│   └── LanguageContext.tsx      ← 语言管理逻辑
├── components/
│   └── LanguageSwitch.tsx       ← 切换按钮组件
└── pages/
    └── _app.tsx                 ← 启用多语言
```

---

## 🎓 5分钟实战练习

### 练习1：添加一个新按钮（带翻译）

1. **添加翻译**（`src/config/translations.ts`）

```typescript
zh: {
  common: {
    // ... 其他翻译
    download: '下载'  // 添加这行
  }
},
en: {
  common: {
    // ... 其他翻译
    download: 'Download'  // 添加这行
  }
}
```

2. **使用翻译**

```typescript
import { useLanguage } from '@/contexts/LanguageContext';

export default function MyPage() {
  const { t } = useLanguage();
  
  return (
    <button>{t.common.download}</button>
  );
}
```

3. **测试**
   - 保存文件
   - 在浏览器中切换语言
   - 确认按钮文字变化

**完成！** 🎉

---

## 💡 专业提示

### 提示1：使用 TypeScript 自动补全

```typescript
const { t } = useLanguage();

// 输入 t. 会自动显示所有可用的翻译
t.common.     // ← 自动补全！
t.dashboard.  // ← 自动补全！
```

### 提示2：检查当前语言

```typescript
const { language } = useLanguage();

if (language === 'zh') {
  // 中文特定逻辑
} else {
  // 英文特定逻辑
}
```

### 提示3：条件渲染

```typescript
{language === 'zh' ? (
  <div>仅中文显示的内容</div>
) : (
  <div>Only shown in English</div>
)}
```

---

## 🎯 总结

### 用户角度
- 点击 🌐 按钮即可切换
- 设置会自动保存

### 开发者角度
1. 导入 `useLanguage`
2. 使用 `{t.xxx.xxx}` 格式
3. 在 `translations.ts` 中添加翻译

**就是这么简单！** 🚀

---

需要更详细的信息？查看 **`多语言功能使用指南.md`** 📚

