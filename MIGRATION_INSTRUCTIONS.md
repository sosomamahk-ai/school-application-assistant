# 迁移说明

## 🎯 迁移步骤

### 步骤 1：创建 translations.json 文件

系统会在首次保存翻译时自动创建 `src/lib/translations.json` 文件。

如果需要手动创建，运行以下命令（在项目根目录）：

```bash
# 创建空文件（系统会自动填充）
touch src/lib/translations.json
echo '{}' > src/lib/translations.json
```

### 步骤 2：更新现有组件

所有使用旧 `useLanguage` hook 的组件需要更新为 `useTranslation`。

**查找需要更新的文件：**
```bash
# 在项目根目录运行
grep -r "useLanguage" src/
```

**更新模式：**
1. 将 `import { useLanguage } from '@/contexts/LanguageContext'` 
   改为 `import { useTranslation } from '@/contexts/TranslationContext'`

2. 将 `const { t } = useLanguage()` 
   改为 `const { t } = useTranslation()`

3. 将 `t.section.key` 
   改为 `t('section.key')`

### 步骤 3：测试

1. 启动开发服务器：`npm run dev`
2. 访问 `/admin/translations`（需要管理员权限）
3. 检查所有翻译是否正确显示
4. 测试语言切换功能
5. 测试翻译编辑和保存

### 步骤 4：部署

1. 提交所有更改到 Git
2. 推送到远程仓库
3. Vercel 会自动部署

## ⚠️ 注意事项

1. **备份现有翻译**
   - 旧的翻译在 `src/config/translations.ts` 中
   - 建议先备份该文件

2. **逐步迁移**
   - 不需要一次性更新所有组件
   - 可以逐步更新，系统会回退到默认翻译

3. **翻译键映射**
   - 旧的嵌套结构：`t.common.save`
   - 新的扁平结构：`t('common.save')`

## 📋 需要更新的文件列表

以下文件需要更新为使用新的翻译系统：

- [x] `src/pages/_app.tsx` - 已更新
- [x] `src/components/Layout.tsx` - 已更新
- [x] `src/components/LanguageSwitch.tsx` - 已更新
- [ ] `src/pages/index.tsx` - 需要更新
- [ ] `src/pages/auth/login.tsx` - 需要更新
- [ ] `src/pages/auth/register.tsx` - 需要更新
- [ ] `src/pages/dashboard.tsx` - 需要更新
- [x] `src/pages/profile/index.tsx` - 已更新（部分）
- [ ] `src/pages/application/[applicationId].tsx` - 需要更新
- [ ] `src/components/AIGuidancePanel.tsx` - 需要更新
- [ ] `src/pages/settings.tsx` - 需要更新
- [ ] `src/pages/admin/templates.tsx` - 需要更新

## 🔄 自动迁移脚本（可选）

如果需要批量更新，可以创建一个脚本，但建议手动更新以确保正确性。

## ✅ 验证清单

迁移完成后，请验证：

- [ ] 所有页面都能正常加载
- [ ] 语言切换功能正常
- [ ] 翻译管理界面可以访问
- [ ] 可以编辑和保存翻译
- [ ] 没有硬编码的文本
- [ ] 所有翻译键都有对应的翻译

