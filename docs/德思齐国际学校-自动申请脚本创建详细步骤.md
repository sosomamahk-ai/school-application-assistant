# 德思齐国际学校（DSC International School）自动申请脚本创建详细步骤

## 📋 概述

本文档将以**德思齐国际学校（DSC International School）**为例，详细介绍如何创建自动申请脚本的每一个步骤，不跳步。

**学校信息：**
- 学校名称：DSC International School（德思齐国际学校）
- 申请页面：https://www.dsc.edu.hk/admissions/application
- 是否需要登录：否（根据网站信息，可以直接访问申请页面）

---

## 🎯 方法一：使用Web界面创建（推荐，最简单）

### 步骤 1：打开项目并启动开发服务器

#### 1.1 打开终端

**Windows 用户：**
1. 按 `Win + X` 键
2. 选择"Windows PowerShell"或"终端"
3. 或者：在项目文件夹中，按住 `Shift` 键，右键点击空白处，选择"在此处打开PowerShell窗口"

**Mac 用户：**
1. 按 `Cmd + 空格键` 打开 Spotlight 搜索
2. 输入"Terminal"
3. 按回车键

#### 1.2 进入项目目录

在终端中输入以下命令（根据您的实际路径修改）：

```bash
cd C:\school-application-assistant
```

> 注意：把 `C:\school-application-assistant` 替换成您的实际项目路径

#### 1.3 启动开发服务器

```bash
npm run dev
```

**等待启动完成**，您会看到类似以下的消息：
```
ready - started server on 0.0.0.0:3000
```

**如果出错：**
- 确保已经安装了依赖：`npm install`
- 确保 Node.js 版本 >= 18：`node --version`

---

### 步骤 2：打开Web管理界面

#### 2.1 打开浏览器

1. 打开浏览器（Chrome、Edge、Firefox 等）
2. 访问：`http://localhost:3000`
3. 如果端口不同，使用终端显示的地址

#### 2.2 登录系统

1. 如果未登录，系统会跳转到登录页面
2. 使用您的管理员账号登录
3. 确保您有管理员权限（role: admin）

#### 2.3 进入自动申请脚本管理页面

**方法一：通过导航栏**
1. 点击顶部导航栏的"管理"或"Admin"
2. 在下拉菜单中找到"自动申请脚本管理"
3. 点击进入

**方法二：直接访问URL**
1. 在浏览器地址栏输入：`http://localhost:3000/admin/auto-apply-scripts`
2. 按回车键

---

### 步骤 3：查找德思齐国际学校的学校ID

#### 3.1 查看学校列表

在"自动申请脚本管理"页面，您会看到两个列表：
- **学校模板列表**（上方）：显示所有可用的学校模板
- **已创建脚本列表**（下方）：显示已经创建的脚本

#### 3.2 找到德思齐国际学校

在"学校模板列表"中：
1. 查找"德思齐国际学校"或"DSC International School"
2. 如果列表很长，可以使用浏览器的搜索功能（`Ctrl + F` 或 `Cmd + F`）
3. 找到后，记录以下信息：
   - **学校ID（schoolId）**：例如 `dsc-international-school` 或 `dsc-hkis-2025`
   - **学校名称**：DSC International School 或 德思齐国际学校

**如果找不到学校：**
- 说明数据库中还没有这个学校的模板
- 需要先创建学校模板（见"方法二"的步骤1）

---

### 步骤 4：获取申请页面URL

#### 4.1 打开学校官网

1. 打开新的浏览器标签页
2. 访问：https://www.dsc.edu.hk
3. 等待页面加载完成

#### 4.2 找到申请页面

根据网站信息，申请页面可能在以下位置：

1. **通过导航菜单：**
   - 点击顶部菜单的"Admissions"（招生）
   - 在下拉菜单中找到"Application Form"（申请表格）
   - 点击进入

2. **直接访问：**
   - 根据网站结构，申请页面可能是：`https://www.dsc.edu.hk/admissions/application`
   - 或者：`https://www.dsc.edu.hk/admissions/applynow`
   - 在浏览器地址栏输入URL，按回车

#### 4.3 确认申请页面

打开申请页面后，确认：
- ✅ 页面显示申请表单
- ✅ 可以直接填写表单（不需要先登录）
- ✅ 有提交按钮

#### 4.4 复制URL

1. 在浏览器地址栏中，选中完整的URL
2. 按 `Ctrl + C`（Windows）或 `Cmd + C`（Mac）复制
3. 例如：`https://www.dsc.edu.hk/admissions/application`

---

### 步骤 5：创建脚本

#### 5.1 点击"创建新脚本"按钮

在"自动申请脚本管理"页面：
1. 找到右上角的"创建新脚本"按钮（通常是蓝色按钮，带有"+"图标）
2. 点击按钮
3. 会弹出一个创建脚本的对话框

#### 5.2 填写表单

在对话框中填写以下信息：

1. **选择学校：**
   - 点击"选择学校"下拉框
   - 在列表中找到"德思齐国际学校"或"DSC International School"
   - 点击选择

2. **申请页面URL：**
   - 在"申请页面URL"输入框中
   - 粘贴您刚才复制的URL
   - 例如：`https://www.dsc.edu.hk/admissions/application`

3. **需要登录：**
   - 根据我们之前的分析，DSC International School 不需要登录
   - **不要勾选**"需要登录"选项
   - 如果申请页面要求先登录，则勾选此选项

#### 5.3 点击"创建脚本"按钮

1. 检查所有信息是否正确
2. 点击对话框底部的"创建脚本"按钮
3. 等待创建完成（可能需要几秒钟）

---

### 步骤 6：查看创建结果

#### 6.1 成功情况

如果创建成功，您会看到：
- ✅ 绿色成功消息："脚本创建成功！文件位置：src/modules/auto-apply/schools/xxx.ts"
- ✅ 脚本出现在"已创建脚本列表"中
- ✅ 显示"已注册"状态

#### 6.2 失败情况

如果创建失败，您会看到：
- ❌ 红色错误消息，说明失败原因
- 常见错误：
  - "脚本文件已存在"：说明已经创建过，需要先删除
  - "找不到选中的模板"：说明学校ID不正确
  - "请选择学校并填写申请URL"：说明信息不完整

**解决方法：**
- 根据错误消息调整信息
- 重新填写表单并创建

---

### 步骤 7：验证脚本已创建

#### 7.1 在Web界面验证

1. 在"已创建脚本列表"中
2. 找到刚创建的脚本
3. 确认显示：
   - ✅ 学校名称：DSC International School
   - ✅ 申请URL：https://www.dsc.edu.hk/admissions/application
   - ✅ 状态：已注册

#### 7.2 在文件系统中验证

1. 打开文件管理器（Windows资源管理器或Mac Finder）
2. 导航到项目目录：`C:\school-application-assistant`
3. 依次打开文件夹：
   - `src`
   - `modules`
   - `auto-apply`
   - `schools`
4. 查看是否有新创建的文件，文件名类似：`dsc-international-school.ts` 或 `dsc-hkis-2025.ts`

---

## 🛠️ 方法二：使用命令行创建（高级用户）

如果您更喜欢使用命令行，或者Web界面不可用，可以使用以下方法：

### 步骤 1：查找学校ID

#### 1.1 打开数据库查看工具

在终端中运行：

```bash
npx prisma studio
```

#### 1.2 查看学校信息

1. 浏览器会自动打开 Prisma Studio（通常是 http://localhost:5555）
2. 在左侧找到 `SchoolFormTemplate` 表
3. 点击打开
4. 在列表中查找"德思齐国际学校"或"DSC International School"
5. 找到后，查看 `schoolId` 列的值
6. **复制这个值**，例如：`dsc-international-school` 或 `dsc-hkis-2025`

#### 1.3 关闭 Prisma Studio

查看完信息后，可以关闭浏览器标签页，但**不要关闭终端**（Prisma Studio 仍在运行）

---

### 步骤 2：准备申请URL

参考"方法一"的步骤4，获取申请页面URL：
- `https://www.dsc.edu.hk/admissions/application`

---

### 步骤 3：运行创建脚本命令

#### 3.1 打开新的终端窗口

**重要：** 如果 Prisma Studio 正在运行，需要打开**新的终端窗口**来运行命令。

#### 3.2 进入项目目录

```bash
cd C:\school-application-assistant
```

#### 3.3 运行创建命令

```bash
node scripts/create-school-script.js dsc-international-school "DSC International School" "https://www.dsc.edu.hk/admissions/application"
```

**参数说明：**
- `dsc-international-school`：学校ID（替换成您从数据库中找到的实际值）
- `"DSC International School"`：学校名称
- `"https://www.dsc.edu.hk/admissions/application"`：申请页面URL

#### 3.4 查看结果

如果成功，您会看到：

```
✅ 脚本文件已创建:
   C:\school-application-assistant\src\modules\auto-apply\schools\dsc-international-school.ts

📝 下一步:
1. 编辑脚本文件，根据实际页面调整:
   - 申请URL
   - 登录逻辑（如果需要）
   - 字段映射（如果自动匹配失败）
   - 特殊字段处理

2. 在 src/modules/auto-apply/autoApplyService.ts 中注册脚本:
   import { dscInternationalSchoolScript } from "./schools/dsc-international-school";
   scriptRegistry[dscInternationalSchoolScript.id] = dscInternationalSchoolScript;

3. 测试脚本:
   - 设置 PLAYWRIGHT_HEADLESS=false
   - 启动开发服务器
   - 在前端测试自动申请功能
```

---

### 步骤 4：注册脚本到系统

#### 4.1 打开脚本注册文件

1. 打开代码编辑器（VS Code 等）
2. 打开文件：`src/modules/auto-apply/autoApplyService.ts`

#### 4.2 添加导入语句

1. 找到文件顶部的导入部分（大约第1-20行）
2. 在最后一个 `import` 语句后面添加：

```typescript
import { dscInternationalSchoolScript } from "./schools/dsc-international-school";
```

**注意：** 变量名可能不同，根据创建脚本时的输出调整。

#### 4.3 注册到脚本表

1. 找到 `scriptRegistry` 对象（大约第40-60行）
2. 在对象中添加您的脚本：

```typescript
const scriptRegistry: SchoolScriptMap = {
  // ... 其他脚本
  [dscInternationalSchoolScript.id]: dscInternationalSchoolScript,
};
```

#### 4.4 保存文件

按 `Ctrl + S`（Windows）或 `Cmd + S`（Mac）保存文件。

---

## 🧪 步骤 8：测试脚本

### 8.1 设置测试环境

#### 8.1.1 创建或编辑环境变量文件

1. 在项目根目录（`C:\school-application-assistant`）创建或编辑文件：`.env.local`
2. 添加以下内容：

```env
PLAYWRIGHT_HEADLESS=false
AUTO_APPLY_SCREENSHOTS=tmp/auto-apply
```

**说明：**
- `PLAYWRIGHT_HEADLESS=false`：让浏览器显示出来，可以看到操作过程
- `AUTO_APPLY_SCREENSHOTS=tmp/auto-apply`：错误时保存截图的位置

#### 8.1.2 保存文件

保存 `.env.local` 文件。

---

### 8.2 启动开发服务器

如果服务器未运行，在终端中运行：

```bash
npm run dev
```

等待启动完成。

---

### 8.3 通过Web界面测试

#### 8.3.1 打开测试页面

1. 打开浏览器
2. 访问：`http://localhost:3000`
3. 登录系统

#### 8.3.2 进入可申请学校页面

1. 点击导航栏的"可申请学校"或"Schools"
2. 或直接访问：`http://localhost:3000/schools`

#### 8.3.3 找到德思齐国际学校

1. 在列表中找到"DSC International School"或"德思齐国际学校"
2. 如果列表很长，使用搜索功能

#### 8.3.4 点击"自动申请"按钮

1. 找到学校行中的"自动申请"按钮
2. 点击按钮
3. **浏览器会自动打开**（因为设置了 `PLAYWRIGHT_HEADLESS=false`）

#### 8.3.5 观察自动化过程

您会看到：
1. ✅ 浏览器自动打开申请页面
2. ✅ 页面自动加载完成
3. ✅ 表单字段自动填写
4. ✅ 提交按钮自动点击
5. ✅ 页面跳转到确认页面（如果成功）

---

### 8.4 查看测试结果

#### 8.4.1 在浏览器中查看

- **成功时：** 页面会显示成功消息，例如："申请已成功提交"
- **失败时：** 页面会显示错误消息，说明失败原因

#### 8.4.2 在终端中查看日志

在运行 `npm run dev` 的终端中，查找以 `[auto-apply]` 开头的日志：

```
[auto-apply] 开始执行自动申请
[auto-apply] 导航到申请页面: https://www.dsc.edu.hk/admissions/application
[auto-apply] 页面加载完成
[auto-apply] 开始填写表单，共 15 个字段
[auto-apply] 字段 "First Name" 已填写
[auto-apply] 字段 "Last Name" 已填写
...
[auto-apply] 找到提交按钮，准备提交
[auto-apply] 申请已成功提交！
```

#### 8.4.3 查看错误截图（如果失败）

如果测试失败：
1. 打开项目目录：`C:\school-application-assistant\tmp\auto-apply`
2. 查看最新的截图文件（`.png` 格式）
3. 查看HTML转储文件（`.html` 格式）

---

## 🔧 步骤 9：调整脚本（如果需要）

### 9.1 如果字段无法自动匹配

#### 9.1.1 打开脚本文件

1. 打开文件：`src/modules/auto-apply/schools/dsc-international-school.ts`
2. 找到 `run` 函数中的字段填写部分

#### 9.1.2 添加字段映射

找到以下代码：

```typescript
// 步骤 3: 字段映射（如果需要）
// 如果模板字段ID与页面字段标签不匹配，取消下面的注释并添加映射
/*
const overrides = remapTemplateFields(payload.template, {
  // 模板字段ID -> 页面标签映射
  // english_first_name: { label: "First Name" },
  // english_last_name: { label: "Last Name" },
  // student_email: { label: "Email Address" },
  // student_phone: { label: "Phone Number" },
});
*/
```

取消注释并修改：

```typescript
// 步骤 3: 字段映射
const overrides = remapTemplateFields(payload.template, {
  // 模板字段ID -> 页面标签映射
  english_first_name: { label: "First Name" },
  english_last_name: { label: "Last Name" },
  student_email: { label: "Email" },
  student_phone: { label: "Phone" },
  // 根据实际页面添加更多映射
});

const fields = overrides.length ? overrides : payload.template.fields;
```

#### 9.1.3 修改字段填写代码

将：

```typescript
const fields = payload.template.fields;
await formFiller.fillFields(page, fields);
```

改为：

```typescript
const fields = overrides.length ? overrides : payload.template.fields;
await formFiller.fillFields(page, fields);
```

#### 9.1.4 保存并重新测试

1. 保存文件
2. 重新运行测试（见步骤8）

---

### 9.2 如果需要处理文件上传

#### 9.2.1 查看申请要求

根据 DSC International School 的申请要求，可能需要上传文件：
- 护照副本
- 成绩单
- 推荐信
- 等等

#### 9.2.2 添加文件上传代码

在脚本的 `run` 函数中，找到"步骤 5: 处理特殊字段"部分：

```typescript
// 步骤 5: 处理特殊字段（如果需要）
// TODO: 添加文件上传、日期选择等特殊处理
// const fileInput = page.locator('input[type="file"]');
// if (await fileInput.count()) {
//   await fileInput.setInputFiles('/path/to/file.pdf');
// }
```

取消注释并修改：

```typescript
// 步骤 5: 处理文件上传
const fileInputs = await page.locator('input[type="file"]').all();
for (let i = 0; i < fileInputs.length; i++) {
  const fileInput = fileInputs[i];
  // 根据字段标签或名称确定上传哪个文件
  const label = await fileInput.evaluate(el => {
    const label = el.closest('label')?.textContent || '';
    return label;
  });
  
  // 根据标签匹配文件（需要在 payload.metadata 中提供文件路径）
  if (label.includes('Passport') && payload.metadata?.passportPath) {
    await fileInput.setInputFiles(payload.metadata.passportPath);
  } else if (label.includes('Transcript') && payload.metadata?.transcriptPath) {
    await fileInput.setInputFiles(payload.metadata.transcriptPath);
  }
  // 添加更多文件匹配逻辑
}
```

#### 9.2.3 保存并重新测试

1. 保存文件
2. 确保在申请时提供了文件路径
3. 重新运行测试

---

## ✅ 完成检查清单

创建脚本后，确认以下项目：

- [ ] 脚本文件已创建：`src/modules/auto-apply/schools/dsc-international-school.ts`
- [ ] 脚本已在 `autoApplyService.ts` 中注册
- [ ] 脚本中的 `id` 与数据库中的 `schoolId` 完全一致
- [ ] 申请URL正确：`https://www.dsc.edu.hk/admissions/application`
- [ ] 已设置 `.env.local` 文件（`PLAYWRIGHT_HEADLESS=false`）
- [ ] 已测试基本流程（打开页面、填写字段、提交）
- [ ] 已查看日志，确认没有错误
- [ ] 字段映射正确（如果使用了自定义映射）
- [ ] 文件上传功能正常（如果需要）

---

## 🐛 常见问题解决

### Q1: 找不到学校ID

**解决方法：**
1. 使用 `npx prisma studio` 查看数据库
2. 在 `SchoolFormTemplate` 表中查找
3. 或者查看 `School` 表

### Q2: 脚本创建失败，提示"文件已存在"

**解决方法：**
1. 删除已存在的脚本文件
2. 或者在 Web 界面中删除已创建的脚本
3. 重新创建

### Q3: 字段无法自动匹配

**解决方法：**
1. 查看终端日志，了解哪些字段匹配失败
2. 使用浏览器开发者工具检查字段的实际标签
3. 添加字段映射（见步骤9.1）

### Q4: 页面加载超时

**解决方法：**
1. 在脚本中增加等待时间
2. 检查网络连接
3. 确认URL正确

### Q5: 提交按钮找不到

**解决方法：**
1. 设置 `PLAYWRIGHT_HEADLESS=false`，观察页面
2. 查看截图：`tmp/auto-apply/` 目录
3. 手动检查页面，找到提交按钮的实际选择器
4. 修改 `locateSubmitButton` 函数

---

## 📚 相关文档

- [零基础创建自动申请脚本指南.md](./零基础创建自动申请脚本指南.md) - 更详细的说明
- [自动申请功能使用指南.md](./自动申请功能使用指南.md) - 功能使用说明
- [AUTO_APPLY_SCRIPT_GUIDE.md](./AUTO_APPLY_SCRIPT_GUIDE.md) - 开发指南

---

## 🎉 完成！

恭喜！您已经成功为德思齐国际学校创建了自动申请脚本。

**下一步：**
1. 继续测试和优化脚本
2. 为其他学校创建脚本
3. 处理更复杂的申请流程（登录、注册等）

如有问题，请查看相关文档或检查终端日志。

